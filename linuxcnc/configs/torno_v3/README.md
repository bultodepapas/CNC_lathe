# Configuración activa `torno_v3`

Estos archivos se copiaron de `/home/cnc/linuxcnc/configs/torno_v3` el
2026-08-29 mientras LinuxCNC estaba ejecutando `torno_v3.ini`.

La selección excluye respaldos internos, caches Python y la variante QtDragon
HD no utilizada. Todos ellos permanecen en el respaldo completo excluido de
Git. No ejecute PNCconf sobre esta carpeta sin revisar el diff: `torno_v3.hal` y
`custom.hal` contienen cambios manuales posteriores al proyecto `.pncconf`.
