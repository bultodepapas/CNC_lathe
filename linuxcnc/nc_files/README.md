# Programas y códigos M personalizados

M100–M104 se copiaron de la máquina y allí tenían permisos `0755`.

Advertencia: estos scripts pertenecen a una implementación anterior de la
torreta y usan señales HAL que no existen en el `custom.hal` actual basado en
`carousel.0`. Se conservan para trazabilidad, pero no deben ejecutarse hasta
retirarlos o adaptarlos explícitamente al control vigente.
